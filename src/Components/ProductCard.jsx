export default function ProductCard({
  productID,
  title,
  description,
  price,
  discountPercentage,
  rating,
  stock,
  image,
  timeRemaining,
  isHighestRated,
}) {
  return <div>{title}</div>;
}
